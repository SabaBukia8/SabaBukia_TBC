<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="12dp"
    android:backgroundTint="@color/white"
    tools:context=".MainActivity">

    <EditText
        android:id="@+id/numberET"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:hint="@string/enter_a_number_1_1000"
        android:inputType="number"
        android:maxLines="1"
        android:maxLength="4"
        android:gravity="center"
        android:textColor="@color/red"
        android:padding="16dp"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        ></EditText>
    <Button
        android:id="@+id/spellOutButton"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="@string/spell_out"
        android:textAllCaps="false"
        android:textColor="@color/red"
        android:backgroundTint="@color/black"
        app:layout_constraintTop_toBottomOf="@+id/numberET"
        app:layout_constraintLeft_toLeftOf="@+id/numberET"
        app:layout_constraintRight_toRightOf="@+id/numberET"
        app:layout_constraintWidth_percent="0.6"
        android:layout_marginTop="12dp"
        ></Button>
    <TextView
        android:id="@+id/result"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="@string/result"
        android:textSize="18sp"
        android:textStyle="bold"
        android:gravity="center"
        android:padding="12dp"
        android:textColor="@color/red"
        app:layout_constraintTop_toBottomOf="@+id/spellOutButton"
        app:layout_constraintLeft_toLeftOf="@+id/numberET"
        app:layout_constraintRight_toRightOf="@+id/numberET"
        android:layout_marginTop="12dp"
        android:minHeight="60dp"

        ></TextView>

    <com.google.android.material.switchmaterial.SwitchMaterial
        android:id="@+id/languageSwitch"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/english"
        android:checked="false"
        app:layout_constraintTop_toBottomOf="@id/result"
        app:layout_constraintStart_toStartOf="@id/result"
        app:layout_constraintEnd_toEndOf="@id/result"
        android:layout_marginTop="16dp"></com.google.android.material.switchmaterial.SwitchMaterial>

</androidx.constraintlayout.widget.ConstraintLayout>